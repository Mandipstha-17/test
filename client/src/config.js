// Configuration for API endpoints
const config = {
  // Your computer's IP address for cross-device access
  // Change this to "http://localhost:3000" when testing on the same device
  API_BASE_URL: "http://192.168.18.108:3000",
  
  // Alternative: Use environment variable if available
  // API_BASE_URL: process.env.REACT_APP_API_URL || "http://localhost:3000",
};

export default config;
